# Security

Report vulnerabilities privately through the GitHub repository's security advisory form after replacing the placeholder repository owner.

Idea Jar runs as trusted Host code with the installing user's permissions. Its browser API accepts same-origin POST requests only, bounds request bodies, validates all wire fields, and stores favorites through the DSH Settings provider. Model calls use the user's configured DSH provider and credentials; the plugin does not read or transmit credential values itself.
